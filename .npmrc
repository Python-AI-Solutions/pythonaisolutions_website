# Disable package-lock.json generation
# We're avoiding package-lock.json to reduce git noise and merge conflicts
# Dependencies in package.json use specific versions where stability is needed
package-lock=false

# Use exact versions when installing packages
save-exact=true
